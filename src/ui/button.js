/* Boutons partages. */
export const primary=(text,attrs)=>`<button class="button primary" ${attrs}>${text}<span aria-hidden="true">↗</span></button>`;
export const textButton=(text,attrs)=>`<button class="text-button" ${attrs}>${text}<span aria-hidden="true">↗</span></button>`;
